# GitHub Push Commands

## Sizning Username'ingiz: osarovasadbek195-wq

### Agar repository nomi `ielts` bo'lsa:
```bash
cd d:\flutter\ielts_sat_prep_app
git remote remove origin
git remote add origin https://github.com/osarovasadbek195-wq/ielts.git
git push -u origin main
```

### Agar repository nomi `ielts-sat` bo'lsa:
```bash
cd d:\flutter\ielts_sat_prep_app
git remote remove origin
git remote add origin https://github.com/osarovasadbek195-wq/ielts-sat.git
git push -u origin main
```

### Agar repository nomi `ielts_sat_prep` bo'lsa:
```bash
cd d:\flutter\ielts_sat_prep_app
git remote remove origin
git remote add origin https://github.com/osarovasadbek195-wq/ielts_sat_prep.git
git push -u origin main
```

### Agar repository nomi boshqa bo'lsa:
Yuqoridagi commandlarda oxirgi qismdagi repository nomini o'zgartiring:
`https://github.com/osarovasadbek195-wq/REPOSITORY_NAME.git`

REPOSITORY_NAME o'rniga GitHub da ochgan repository nomingizni yozing.

### Authentication xatosi bersa:
```bash
git config --global user.name "osarovasadbek195-wq"
git config --global user.email "your-email@example.com"
```

Keyin qayta push qiling: `git push -u origin main`
